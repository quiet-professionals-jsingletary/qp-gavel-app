 /**---------------------------------------------------------------------------------------------------- ->
*  ┌────────────────────────────────────┐
*  │ |> GAVEL - Feature Layer Builder   │
*  └────────────────────────────────────┘
*
*  @description:   'Custom service allows user to query a single `RegId` and rendering all data points'
*  @implements:    'JSON data leveraging the JavaScript API'
*  @returns:       'JSON data object and Feature Layer Geometry that renders out all data for the given RegId'
*
*/

export const patternOfLifeService = () => {

  
}